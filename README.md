# DevOps-TaskManager-Cloud
A task management application (ToDo List) with an API in Python (Flask/FastAPI), running inside a Docker container on AWS EC2, and connected to S3 for file storage. The entire project is managed with Terraform, and CI/CD is implemented via GitHub Actions.
